# 영어 단어장 웹 앱 실행 방법

## 🚀 가장 간단한 방법 (3단계)

### 1단계: PowerShell 열기
- **Windows 키 + X** 눌러서 메뉴 열기
- **"Windows PowerShell"** 또는 **"터미널"** 클릭

### 2단계: 다음 명령어들을 순서대로 입력하고 Enter 누르기

```powershell
cd C:\Users\Admin\projects\game_english
python web_vocab_app.py
```

### 3단계: 브라우저에서 접속
브라우저를 열고 주소창에 입력:
```
http://127.0.0.1:5000
```

---

## 📝 상세한 설명

### PowerShell 창이 열리면:

1. **프로젝트 폴더로 이동:**
   ```
   cd C:\Users\Admin\projects\game_english
   ```
   - 이 명령어는 프로젝트 폴더로 이동합니다
   - Enter 키를 누르세요

2. **Flask 앱 실행:**
   ```
   python web_vocab_app.py
   ```
   - 이 명령어는 웹 앱을 시작합니다
   - Enter 키를 누르세요

3. **성공 메시지 확인:**
   다음과 같은 메시지가 보이면 성공입니다:
   ```
   ============================================================
   영어 단어장 웹 애플리케이션 시작!
   ============================================================
   단어 개수: 6개
   통계 기록: 2개
   접속 주소: http://127.0.0.1:5000
   ============================================================
   
   * Running on http://127.0.0.1:5000
   * Debug mode: on
   ```

4. **중요:** 이 메시지가 보이는 동안 PowerShell 창을 **닫지 마세요!**
   - 창을 닫으면 웹사이트에 접속할 수 없습니다
   - 종료하려면 PowerShell 창에서 **Ctrl + C**를 누르세요

5. **브라우저 열기:**
   - Chrome, Edge, Firefox 등 아무 브라우저나 열기
   - 주소창에 입력: `http://127.0.0.1:5000`
   - Enter 키 누르기

---

## 🎯 더 쉬운 방법 (배치 파일)

### 방법 1: 파일 탐색기에서
1. 파일 탐색기에서 `C:\Users\Admin\projects\game_english` 폴더 열기
2. `run_web_app.bat` 파일을 더블클릭
3. 브라우저에서 `http://127.0.0.1:5000` 접속

### 방법 2: PowerShell에서
```powershell
cd C:\Users\Admin\projects\game_english
.\run_web_app.bat
```

---

## 🔍 실행이 안 될 때 체크리스트

### 1. Python이 설치되어 있나요?
```powershell
python --version
```
- 버전 번호가 나오면 OK (예: Python 3.13.9)
- "Python을 찾을 수 없습니다" 오류가 나오면 Python 설치 필요

### 2. Flask가 설치되어 있나요?
```powershell
pip list | findstr flask
```
- "flask"가 나오면 OK
- 없다면 설치: `pip install flask`

### 3. 파일들이 모두 있나요?
다음 파일들이 있어야 합니다:
- `web_vocab_app.py`
- `templates/index.html`
- `static/style.css`
- `static/script.js`

---

## 💡 사용 팁

### 웹 앱 종료하는 방법
1. PowerShell 창에서 **Ctrl + C** 누르기
2. 또는 PowerShell 창 닫기

### 다시 실행하는 방법
1. PowerShell 창에서 Ctrl + C로 종료
2. 다시 `python web_vocab_app.py` 입력
3. 브라우저 새로고침 (F5)

### 여러 번 실행하려면
- 같은 PowerShell 창에서 Ctrl + C로 종료 후 다시 실행 가능
- 다른 PowerShell 창을 새로 열 수도 있음

---

## 🎬 실제 실행 예시

PowerShell에서 실행하면 다음과 같이 보입니다:

```powershell
PS C:\Users\Admin> cd C:\Users\Admin\projects\game_english
PS C:\Users\Admin\projects\game_english> python web_vocab_app.py
============================================================
영어 단어장 웹 애플리케이션 시작!
============================================================
단어 개수: 6개
통계 기록: 2개
접속 주소: http://127.0.0.1:5000
============================================================

 * Serving Flask app 'web_vocab_app'
 * Debug mode: on
WARNING: This is a development server. Do not use it in a production deployment.
 * Running on http://127.0.0.1:5000
Press CTRL+C to quit
```

이 상태에서 브라우저로 접속하면 됩니다!

---

## 📌 요약

1. PowerShell 열기
2. `cd C:\Users\Admin\projects\game_english` 입력
3. `python web_vocab_app.py` 입력
4. "Running on http://127.0.0.1:5000" 메시지 확인
5. 브라우저에서 `http://127.0.0.1:5000` 접속
6. **PowerShell 창은 열어둬야 합니다!**

---

**이제 바로 실행해보세요!** 🚀

